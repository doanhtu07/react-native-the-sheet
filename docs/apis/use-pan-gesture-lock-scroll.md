# usePanGestureLockScroll

A hook that should be used in a component under `BottomSheet`

It's simply an effect that will lock scroll aggressively whenever bottom sheet is translating and user is not actively panning
