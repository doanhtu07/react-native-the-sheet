# useBottomSheetPanGesture

A hook that provides a factory function to create a pan gesture handler for the bottom sheet

You can just throw this handler into a `GestureDetector` in case you need to create a custom component and don't want to manually copy all the code of pan gesture handling from the library

It provides:

- `() => PanGesture`
